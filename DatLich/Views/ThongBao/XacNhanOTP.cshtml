<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>ĐĂNG NHẬP</title>
    <style>
        body {
        }

        .input-group {
            display: flex;
            flex-direction: column;
            align-items: center;
            border: 1px solid #e0e0e0;
            border-radius: 6px;
            padding: 2rem;
            background-color: #fff;
        }

        .input {
            min-height: 50px;
            width: 300px;
            padding: 0 1rem;
            color: #000;
            font-size: 15px;
            border: 1px solid #5e4dcd;
            border-radius: 6px;
            background-color: transparent;
            margin-bottom: 1rem;
        }

        .button--submit {
            min-height: 50px;
            width: 300px;
            padding: .5em 1em;
            border: none;
            border-radius: 6px;
            background-color: #5e4dcd;
            color: #fff;
            font-size: 15px;
            cursor: pointer;
            transition: background-color .3s ease-in-out;
        }

            .button--submit:hover {
                background-color: #5e5dcd;
            }

        .input:focus,
        .input:focus-visible {
            border-color: #3898EC;
            outline: none;
        }
    </style>
</head>
<body>
    <div class="input-group">
        <h1>ĐĂNG NHẬP</h1>
        <p>Nhập mã OTP được gửi đến @TempData["Email"]</p>
        <form action="@Url.Action("XacNhanOTP","ThongBao")" method="post">
            <input type="text" name="otp" placeholder="Nhập mã OTP" class="input">
            <br />
            <input type="submit" value="Xác nhận" class="button--submit" autocomplete="off">
        </form>
        <h2>@ViewBag.MaDaGui</h2>
        <h2>@ViewBag.Email</h2>
        <h2>@ViewBag.ThongBao</h2>
    </div>
</body>
</html>

