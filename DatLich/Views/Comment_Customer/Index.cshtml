@model IEnumerable<DatLich.Models.Comment_Customer>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer.Customer_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CommentCustomer_Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CommentCustomer_TimeOrder)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Customer_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommentCustomer_Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommentCustomer_TimeOrder)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CommentCustomer_ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.CommentCustomer_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CommentCustomer_ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
